<h2> List of the students in each section by ID </h2>


	<%= @student.first_name %><td>
	<%= @student.last_name %><td>
	<%= @student.age %><td>
	<%= @student.email_id%><td>
	<%= @student.phone_no %><td>
	<%= @student.address %><td><br/><br/>

	<% @subject.each do |sub| %>

	<%= sub.name %><td>
	<%= sub.description %><td>
	<%= sub.credit %><td><br/><br/>

	<% end %>


<% @result.each do |res| %>
<%= res.status%><td>
<%= res.marks %><td>
<%= res.division %><td><br/><br/>

<% end %>


<%= link_to 'Edit', 'students/edit' %>